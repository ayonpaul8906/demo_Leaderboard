export UPDATE_INTERVAL=5          # Update every 30 minutes
export CONCURRENCY=5                # Process 5 users simultaneously
export POLITE_DELAY=0.5            # Wait 0.5 seconds between requests
export AUTO_START=true              # Run initial update on startup